export interface VisualConfModel {
  escutcheon: string;
  palette: string;
  reflect: boolean;
  border: {
    size: number;
  };
  defaultStrokeSize: number;
  outputSize: {
    width: number;
    height: number;
  };
}
